use mod base.*;
use mod base.init.*;

# main := bind[](
# 	bind[](
# 		return[](10),
# 		(a: int) => return[](a * 5)),
# 	(a: int) -> Init[Unit]
# 		=> printIntTwice(a));

# printIntTwice := (val: int) -> Init[Unit]
# 	=> init.bind[](printInt(val),
# 		(a: Unit) => printInt(val));

main := printLn(match Maby[int].Some(3) {
	Maby[int].Some(2)  => "two";
	Maby[int].Some($a) => "something else";
	Maby[int].None     => "nothing";
});
