connect := (connectionString: String) -> base.Init[Connection]
	=> base.init.bind[](
		_dbConnect("postgresql", connectionString),
		(handle: DBConnectionHandle) =>
			base.init.return[](Connection {
				dbKind = "postgresql";
				connectionString = connectionString;
				_handle = handle;
			}));
